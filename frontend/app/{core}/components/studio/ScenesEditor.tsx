import { Project } from "@/{core}/utils/types";

interface ScenesEditorProps {
    project: Project;
}

export default function ScenesEditor({ project }: ScenesEditorProps) {
    return (
        <div>
            <h1>Scenes Editor</h1>
        </div>
    )
}